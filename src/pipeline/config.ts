import "dotenv/config";

// Пустая строка в .env (например, "CHARACTER_REFERENCE_URL=" из шаблона)
// означает "не задано" — иначе она затирала бы значения по умолчанию.
function env(name: string): string | undefined {
  const value = process.env[name]?.trim();
  return value ? value : undefined;
}

function requireEnv(name: string): string {
  const value = env(name);
  if (!value) {
    throw new Error(
      `Отсутствует переменная окружения ${name}. Заполните .env (см. .env.example).`,
    );
  }
  return value;
}

// Эталон внешности персонажа. Kie.ai принимает входные картинки только по
// публичной ссылке, поэтому по умолчанию берём файл из этого же репозитория
// через raw.githubusercontent.com. Если сделаете репозиторий приватным —
// подставьте свою публичную ссылку в CHARACTER_REFERENCE_URL.
const RAW_BASE =
  "https://raw.githubusercontent.com/tipsyblack/Amg/refs/heads/claude/remotion-video-automation-biby8g/assets/characters";

// Эталон внешности. Это shamil-clip.png, а не исходный shamil.png, и разница
// принципиальная: исходник — рекламный стикер на ФОТОГРАФИИ офиса, с лампой и
// надписью PRO NEIRO внизу. Лицо занимало там 19% высоты кадра, то есть после
// сжатия до 720p от него оставалось меньше шестидесяти пикселей — сохранять
// нечего, и модель дорисовывала лицо заново. Отсюда «лицо не похоже».
//
// В shamil-clip.png фотография залита белым (стикер отделён от неё по сплошной
// белой обводке), лампа с надписью отрезана по дымному хвосту, кадр обрезан по
// персонажу: лицо стало 34% высоты — вчетверо больше пикселей. Заодно ушли две
// причины поменьше: модели больше не нужно перерисовывать фон (он уже
// однотонный, как мы и просим в промпте), и в кадр не лезет текст, который мы
// сами же запрещаем.
//
// Собран скриптом scripts/build-character-reference.mjs из исходника — тот
// остался в репозитории и никуда не делся.
const DEFAULT_CHARACTER_REFERENCE_URL = `${RAW_BASE}/shamil-clip.png`;

export const config = {
  // Сценарий — OpenRouter.
  openRouterApiKey: requireEnv("OPENROUTER_API_KEY"),
  // Sonnet 5 вместо gemini-2.5-flash: сценарий — единственный шаг, где
  // решается, будет ли ролик смотрибельным, а стоит он копейки на фоне
  // картинок (~$0.03 против ~$1.35 за 15 картинок). Модель точнее следует
  // нашим жёстким правилам (хук, реклама только в финале, бюджет слов),
  // поэтому реже срабатывает авто-переписывание.
  openRouterModel: env("OPENROUTER_MODEL") ?? "anthropic/claude-sonnet-5",
  // Веб-поиск для сценария (плагин OpenRouter). Нужен, чтобы темы были
  // актуальными, а не пересказом знаний модели: без него «свежие модели и
  // тренды» опираются на дату обучения. Стоит порядка $4 за 1000 результатов,
  // то есть копейки на ролик. SCRIPT_WEB_SEARCH=0 отключает.
  scriptWebSearch: (env("SCRIPT_WEB_SEARCH") ?? "1") !== "0",
  scriptWebSearchResults: Number(env("SCRIPT_WEB_SEARCH_RESULTS") ?? 3),

  // Zernio: привязка и публикация в соцсети. Ключ живёт ТОЛЬКО в .env на
  // сервере — репозиторий публичный. Пусто = интеграция выключена, бот об
  // этом честно скажет вместо того, чтобы падать.
  zernioApiKey: env("ZERNIO_API_KEY") ?? "",
  // Профиль Zernio, внутрь которого подключаются аккаунты. Пусто — возьмём
  // профиль по умолчанию из личного кабинета.
  zernioProfileId: env("ZERNIO_PROFILE_ID") ?? "",
  // Часовой пояс канала: в нём понимается «/schedule 18:00». Сервер почти
  // наверняка живёт по UTC, и без этого вечерний слот уехал бы на несколько
  // часов — а заметили бы это только по охватам.
  postTimezone: env("POST_TIMEZONE") ?? "Europe/Moscow",
  // Проверка сценария моделью-критиком по чек-листу (data/review-checklist.md).
  // Правила вкуса меняются вместе с продуктом и площадкой, и держать их
  // регулярками в коде для контент-завода — тупик: каждая правка требует
  // программиста и деплоя. Критик работает на любой теме и правится текстом.
  // Стоит одну дешёвую генерацию на сценарий. SCRIPT_REVIEW=0 отключает.
  scriptReview: (env("SCRIPT_REVIEW") ?? "1") !== "0",
  // Проверки вкуса регулярками (пустой финал, слабый призыв, устаревшие
  // примеры). Они дешевле критика и срабатывают детерминированно, но ловят
  // формулировку, а не смысл. SCRIPT_TASTE_RULES=0 оставляет только критика и
  // структурные проверки — так имеет смысл работать, когда тем много и
  // регулярки начинают мешать.
  scriptTasteRules: (env("SCRIPT_TASTE_RULES") ?? "1") !== "0",
  // Цель по длине описания под пост. Жёсткий предел — 500 символов
  // (DESCRIPTION_MAX_CHARS), поэтому цель держим ниже: если просить ровно
  // предел, модель через него перескакивает и текст приходится резать.
  descriptionChars: Number(env("DESCRIPTION_CHARS") ?? 450),

  // Озвучка и картинки — Kie.ai (один ключ на оба сервиса).
  kieApiKey: requireEnv("KIE_API_KEY"),
  // Переопределяется только в тестах (см. scripts/test-kie-client.mjs).
  kieApiBase: env("KIE_API_BASE") ?? "https://api.kie.ai/api/v1/jobs",
  kieTtsModel: env("KIE_TTS_MODEL") ?? "elevenlabs/text-to-speech-multilingual-v2",
  // Именно ID голоса, а не имя: в документации Kie.ai пример показывает имя
  // ("Rachel"), но список допустимых значений — ID, и с именем генерация
  // падает с "internal error". По умолчанию — ID голоса Rachel.
  kieTtsVoice: env("KIE_TTS_VOICE") ?? "21m00Tcm4TlvDq8ikWAM",
  // Скорость речи, общая для обоих путей озвучки.
  //
  // 1.13 выведено замером, а не подобрано: наш ролик даёт 2.06 слова в
  // секунду, присланный референс — 2.33, отношение 1.13. Раньше скорость
  // уходила ТОЛЬКО через прокси Kie.ai, а прямой ElevenLabs (которым идёт
  // единая озвучка) её не отправлял вовсе — то есть настройка была, но на
  // рабочем пути не действовала.
  //
  // KIE_TTS_SPEED оставлен как старое имя: у кого он прописан в .env, тот не
  // должен ничего править.
  ttsSpeed: Number(env("TTS_SPEED") ?? env("KIE_TTS_SPEED") ?? 1.13),

  // Настройки голоса ElevenLabs (действуют для оба провайдера). Значения
  // подобраны под клонированный голос: чем выше similarity_boost и чем ниже
  // style, тем ближе результат к исходной записи; speaker boost добавляет
  // сходства с оригинальным тембром. Стабильность ниже 0.5 оставляет живую
  // интонацию — на 1.0 речь становится ровной и «диктором».
  ttsStability: Number(env("TTS_STABILITY") ?? 0.4),
  ttsSimilarityBoost: Number(env("TTS_SIMILARITY_BOOST") ?? 0.9),
  ttsStyle: Number(env("TTS_STYLE") ?? 0),
  ttsSpeakerBoost: (env("TTS_SPEAKER_BOOST") ?? "1") !== "0",
  // Субтитры по словам. Точные тайминги приходят от прямого ElevenLabs вместе
  // со звуком; при провайдере kie слова раскладываются приблизительно.
  wordSubtitles: (env("WORD_SUBTITLES") ?? "1") !== "0",
  // Пусто = модель определяет язык сама.
  kieTtsLanguageCode: env("KIE_TTS_LANGUAGE_CODE") ?? "",

  // Резервная озвучка напрямую через ElevenLabs — нужна, когда прокси Kie.ai
  // для их моделей лежит. Провайдер: "kie" (по умолчанию) или "elevenlabs";
  // переключается командой /tts в боте.
  ttsProvider: (env("TTS_PROVIDER") ?? "kie") as "kie" | "elevenlabs",
  elevenLabsApiKey: env("ELEVENLABS_API_KEY"),
  elevenLabsModelId: env("ELEVENLABS_MODEL_ID") ?? "eleven_multilingual_v2",
  kieImageModel: env("KIE_IMAGE_MODEL") ?? "google/nano-banana-edit",
  // Оживление кадра (image-to-video, Seedance). Пусто — слаг берётся из
  // реестра VIDEO_MODELS; заполненное значение перебивает реестр целиком.
  // Проверить, какой слаг принимает аккаунт, можно командой /vidmodel.
  kieVideoModel: env("KIE_VIDEO_MODEL") ?? "",
  // Сколько сцен в ролике оживлять клипом. 0 — ни одной (по умолчанию): клип
  // стоит примерно четверть доллара за пару секунд, это дороже всей остальной
  // сцены вместе взятой, поэтому включается осознанно — командой /clips.
  clipScenes: Number(env("CLIP_SCENES") ?? 0),
  // Длина клипа. Четыре, а не два: Seedance 2 короче четырёх секунд не делает
  // вовсе (диапазон 4-15), и просьба о двух отвергается запросом. Клип всё
  // равно обычно длиннее сцены — остаток она досиживает на его последнем
  // кадре, так что лишняя секунда не мешает, а вот платим мы посекундно.
  clipSeconds: Number(env("CLIP_SECONDS") ?? 4),
  // Разрешение клипа. В документации Kie.ai примеры для V1 написаны строчной
  // буквой («720p»), а описания Seedance 2 — прописной («720P»). Какой
  // вариант принимает API, видно только по ответу, поэтому значение вынесено
  // сюда: чинится правкой .env, а не передеплоем.
  clipResolution: env("CLIP_RESOLUTION") ?? "720p",
  // Откуда брать клип для сцены:
  //   auto     — готовый из библиотеки маскота, если он есть, иначе оживить
  //              картинку сцены за деньги (по умолчанию);
  //   library  — только библиотека, платных генераций не делать вовсе;
  //   generate — всегда оживлять картинку сцены, библиотеку не трогать.
  clipSource: (env("CLIP_SOURCE") ?? "auto") as "auto" | "library" | "generate",
  // Замок внешности для клипов библиотеки: тот же эталон уходит и последним
  // кадром, поэтому персонаж обязан к концу вернуться ровно к нему. Держит
  // лицо, но делает движение «туда и обратно» — жест возвращается в исходную
  // позу. Включать, если после нового эталона лицо всё равно плывёт.
  // Работает только у Seedance 2.x: у V1 последнего кадра нет.
  clipLockIdentity: (env("CLIP_LOCK_IDENTITY") ?? "0") !== "0",
  // Музыка (Suno через Kie.ai). Считается дольше картинок — свой лимит.
  kieMusicModel: env("KIE_MUSIC_MODEL") ?? "V5",
  // Suno у Kie.ai требует callBackUrl обязательно, но нам он не нужен: мы
  // сами опрашиваем статус задачи. Поэтому здесь заглушка на зарезервированном
  // IANA домене example.com — Kie.ai постучится в пустоту и это ни на что не
  // влияет. Если однажды поднимете свой обработчик, впишите его адрес.
  kieMusicCallbackUrl:
    env("KIE_MUSIC_CALLBACK_URL") ?? "https://example.com/kie-music-callback",
  kieMusicTimeoutMs: Number(env("KIE_MUSIC_TIMEOUT_SECONDS") ?? 600) * 1000,
  kiePollIntervalMs: Number(env("KIE_POLL_INTERVAL_SECONDS") ?? 3) * 1000,
  kieTimeoutMs: Number(env("KIE_TIMEOUT_SECONDS") ?? 600) * 1000,
  // Повторы при сбоях Kie.ai: сколько попыток на задачу и базовая пауза
  // между ними (растёт вдвое с каждой попыткой: 2с, 4с, 8с, 16с).
  kieMaxAttempts: Number(env("KIE_MAX_ATTEMPTS") ?? 5),
  kieRetryBaseMs: Number(env("KIE_RETRY_BASE_SECONDS") ?? 2) * 1000,

  characterReferenceUrl:
    env("CHARACTER_REFERENCE_URL") ?? DEFAULT_CHARACTER_REFERENCE_URL,
  // Сколько сцен показать без карточки — маскот во весь рост на белом. Приём
  // из референса: там карточка на этих сценах исчезает совсем, и именно этого
  // у нас не было — маскот всегда сидел ВНУТРИ рамки. Одна сцена на ролик по
  // умолчанию: приём сильный, но от повторения стирается, и в референсе он
  // тоже встречается один раз. Ставится на финал — там маскот обращается к
  // зрителю с призывом. 0 отключает.
  mascotScenes: Number(env("MASCOT_SCENES") ?? 1),
  // Маскот по умолчанию только в хуке и финале: в каждой сцене он превращал
  // ролик в галерею поз одного персонажа. =1 возвращает его во все сцены.
  characterEveryScene: (env("CHARACTER_EVERY_SCENE") ?? "0") !== "0",

  // Брендовая концовка — последний кадр ролика: тёмная плашка с логотипом и
  // названием. Пустое BRAND_NAME отключает её целиком.
  brandName: env("BRAND_NAME") ?? "PRO NEIRO",
  brandTagline: env("BRAND_TAGLINE") ?? "",
  // Файл в public/brand/. Пусто — кадр собирается из одного текста: ставить
  // <Img> на несуществующий файл нельзя, рендер упадёт на последнем кадре.
  brandLogoFile: env("BRAND_LOGO_FILE") ?? "logo.png",
  outroSeconds: Number(env("OUTRO_SECONDS") ?? 2),

  // Громкость готового ролика в LUFS. Снята с референса: все 11 присланных
  // агентских роликов лежат на −9.8…−12.2 LUFS, наш первый ролик измерялся на
  // −14.6. Музыки в референсе нет вовсе — «фон» там делает именно громкость.
  // MASTER_LUFS=0 отключает выравнивание.
  masterLufs: Number(env("MASTER_LUFS") ?? -12),

  // 60 кадров: резкие стыки длятся 0.3 с, и на 30 fps это всего 9 кадров —
  // движение читается рвано. В референсе 60. Платим временем рендера: оно
  // примерно удваивается. VIDEO_FPS=30 возвращает как было.
  fps: Number(env("VIDEO_FPS") ?? 60),
  // Сколько сцен максимум и в какую длину должен уложиться ролик.
  maxScenes: Number(env("MAX_SCENES") ?? 15),
  maxVideoSeconds: Number(env("MAX_VIDEO_SECONDS") ?? 60),
  width: Number(env("VIDEO_WIDTH") ?? 1080),
  height: Number(env("VIDEO_HEIGHT") ?? 1920),
};
