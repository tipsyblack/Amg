import { config } from "./config";
import { getImageModel } from "./imageModels";
import { runKieTask } from "./kie";

// Стиль сцены выведен из присланного референс-видео: плоская векторная
// иллюстрация, толстый чёрный контур, приглушённая пастельная палитра,
// лёгкий юмористический тон. Персонаж — фирменный маскот "Pro Neiro":
// джин Шамиль (Шама). Правьте под свой конкретный референс/бренд-бук.
// Про палитру здесь сказано ровно столько, сколько относится к манере рисунка:
// цвета приглушённые. КАКИЕ именно — задаёт тон конкретной сцены (sceneLook).
// Раньше тут стояла «приглушённая пастельная палитра», и модель разрешала это
// всегда одинаково: 12 картинок из 15 в готовом ролике оказались в одном
// секторе тона 30-45°, песочный беж. Слово «пастельная» вдобавок спорило бы с
// ночными сценами.
//
// «Заполняет кадр целиком» — про поля и рамки, а не про плотность: плотность
// тоже задаётся сценой, и у референса она гуляет от 31% до 100%.
export const STYLE_PROMPT = `Плоская векторная иллюстрация в стиле коротких
объясняющих видео для соцсетей: толстый ровный чёрный контур, приглушённые
неяркие цвета, простые обтекаемые формы, лёгкий юмористический тон,
современный flat-cartoon стиль. Вертикальный кадр, иллюстрация заполняет его
целиком — без полей, рамок и белых краёв по бокам.`;

/**
 * Описание маскота. Отделено от стиля намеренно: персонаж нужен не в каждой
 * сцене.
 *
 * Раньше это был один текст со словами «главный персонаж», и эталон внешности
 * прикладывался к каждому запросу — джин честно лез в кадр даже там, где сцена
 * про серверы или про космос, и ролик превращался в галерею поз одного
 * человека. В референсе иллюстрации в основном без маскота: он появляется
 * отдельным слоем в ключевых местах.
 */
export const CHARACTER_PROMPT = `В кадре — джин по имени Шамиль (Шама),
фирменный маскот бренда "Pro Neiro": синяя кожа, чёрные волосы собраны в хвост
с золотым зажимом, острые уши, тонкие усы и аккуратная бородка, золотая
серьга-кольцо, мускулистый торс, красный жилет с золотой окантовкой, золотые
браслеты на запястьях. Обычно появляется в виде клубящегося дымного
шлейфа/силуэта — как будто выходит из волшебной лампы. Первое приложенное
изображение — эталон его внешности: сохраняй лицо, причёску, одежду и цветовую
гамму, меняя только позу, ракурс, окружение и действие по смыслу сцены. Лампу
с логотипом в кадр не добавляй. Фон эталона намеренно пустой и белый — это
служебный кадр, а не образец фона: окружение сцены бери из её описания.`;

/** Сцена без маскота: показываем предмет, место или явление, а не человека. */
export const NO_CHARACTER_PROMPT = `Персонажей-людей и маскотов в кадре нет.
Смысл сцены показывай предметом, местом или явлением: техникой, схемой,
пейзажем, крупным планом объекта. Это иллюстрация к мысли, а не портрет.`;

/**
 * Запрет надписей на картинке. Идёт в промпте последним — так у него больше
 * веса: модели любят подписывать сцену сами.
 *
 * Причина запрета: подпись сцены рисует Remotion под карточкой, чистым
 * шрифтом с кириллицей. Когда ту же мысль дублирует текст внутри картинки,
 * кадр читается дважды и выглядит неряшливо — тем более что кириллицу
 * картиночные модели рисуют с ошибками.
 */
export const NO_TEXT_RULE = `КРИТИЧНО, важнее остальных указаний: в кадре не
должно быть НИКАКОГО текста, надписей и букв. Подпись к сцене добавляется
отдельно при монтаже, поэтому дублировать её на картинке нельзя.

Запрещены: заголовки и слоганы, речевые пузыри и облачка с текстом, вывески,
таблички, ценники, подписи на экранах телефонов и ноутбуков, надписи на
кнопках, обложках книг, коробках и одежде, цифры, логотипы и водяные знаки —
на любом языке, латиницей и кириллицей. ФОРМУЛЫ И УРАВНЕНИЯ тоже запрещены:
математическая запись — это тоже текст, и она появляется особенно охотно,
когда сцена про вычисления. Считающую машину показывай схемой и стрелками, а
не написанной формулой.

Смысл сцены передавай только изображением: позой и мимикой персонажа,
предметами, жестами, простыми знаками (галочка, крестик, стрелка, восклицание,
шестерёнка, сердечко). Экраны устройств, вывески и листы бумаги оставляй
пустыми или заполняй абстрактными полосками и блоками без букв.`;

interface GenerateSceneImageOptions {
  prompt: string;
  outFile: string;
  // Ссылка на картинку предыдущей сцены — передаётся вторым референсом,
  // чтобы окружение и палитра не "уплывали" между сценами.
  previousSceneUrl?: string;
  // Ключ модели из IMAGE_MODELS (выбор в боте); пусто = модель по умолчанию.
  modelKey?: string;
  // Нужен ли в кадре маскот. Эталон его внешности прикладывается только когда
  // нужен: приложенная картинка — сильный сигнал, и модель тянет персонажа в
  // кадр, даже если промпт просит обойтись без него.
  withCharacter?: boolean;
}

/**
 * Генерирует иллюстрацию сцены через Kie.ai и сохраняет её в outFile.
 * Возвращает публичную ссылку на результат — её передаём следующей сцене.
 */
export async function generateSceneImage({
  prompt,
  outFile,
  previousSceneUrl,
  modelKey,
  withCharacter = false,
}: GenerateSceneImageOptions): Promise<string> {
  const spec = getImageModel(modelKey);
  const imageUrls = withCharacter ? [config.characterReferenceUrl] : [];
  if (previousSceneUrl) {
    imageUrls.push(previousSceneUrl);
  }

  return runKieTask({
    model: spec.model,
    input: spec.buildInput(prompt, imageUrls),
    outFile,
    label: `иллюстрация сцены, модель ${spec.model}`,
  });
}
